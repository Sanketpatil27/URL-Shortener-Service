<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>

    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
    </style>
</head>
<body>
    <center>
            
        <h1>URL Shortner</h1>
        
        <!-- if server has send the ID: -->
        <% if(locals.id) { %>
            <p>URL Generated: http://localhost:3000/url/<%=id %></p>
        <% } %>

        <div>
            <form action="/url" method="post" id="form">
                <label for="redirectURL">Enter Your Long URL</label>
                <input type="text" id="redirectURL" name="url" placeholder="http://example.com">
                <button id="generate" type="submit">Generate</button>
            </form>
        </div>

        <br> <br>


        <div>
            <% if(locals.urls) { %>
                <table border>
                    <thead>
                        <th>S. No</th>
                        <th>ShortID</th>
                        <th>Redirect</th>
                        <th>TotalClicks</th>
                    </thead>
                    <tbody>
                        <% urls.forEach((url, ind) => { %>
                            <tr>
                                <td> <%= ind+1 %> </td>
                                <td> <%= url.shortId %> </td>
                                <td> <%= url.redirectURL %> </td>
                                <td> <%= url.visitHistory.length %> </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } %>
        </div>
    </center>

</body>
</html>